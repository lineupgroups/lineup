/**
 * Re-export the useProjectContext hook from the context file
 * for cleaner imports in components
 */
export { useProjectContext } from '../contexts/ProjectContext';
export type { ProjectContextType } from '../contexts/ProjectContext';
